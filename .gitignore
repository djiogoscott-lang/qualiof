# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
.turbo/
dist/
build/
out/

# Env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Claude Code local config (mémoire, hooks, settings utilisateur)
.claude/

# Lockfile npm parasite (le projet utilise pnpm)
/package-lock.json

# Prisma
packages/db/prisma/migrations/dev.db*

# Données sensibles RGPD — snapshots de production (apprenants, SIRET, n° SS, etc.)
# Caches locaux d'imports, jamais versionnés même en repo privé.
packages/db/airtable-snapshot/
*.xlsx

# Tests
coverage/
playwright-report/
test-results/

# TypeScript incremental build artifacts (packages only — apps/web is intentionally tracked)
packages/*/tsconfig.tsbuildinfo

# Python (doc-engine)
__pycache__/
*.pyc
.venv/
venv/

# Uploaded user data (palier 1: stays out of git)
data/uploads/

# GSD planning (local-only)
.planning/
.vercel
